import Home from './../tema/productos/componentes/casas.js';
const serverRoutes = [
    {
        path: '/',
        component:Home,
        exact: true,

        
    },
];

export default serverRoutes;